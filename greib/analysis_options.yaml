# تهيئة محلل Flutter لتطبيق "گريب منك"
# نستثني مجلد build/ المولّد (يحوي مصادر اختبارية لـ Firebase pods)
# لكي يبقى `flutter analyze` نظيفًا دون أخطاء من كود خارجي مولّد.
analyzer:
  exclude:
    - build/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"
